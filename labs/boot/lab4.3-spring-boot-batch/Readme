Spring Boot runs schema-@@platform@@.sql automatically during startup. -all is the default for all platforms. 

A common paradigm in batch processing is to ingest data, transform it, and then pipe it out somewhere else.

@EnableBatchProcessing annotation adds many critical beans that support jobs(default uses a memory-based database).

