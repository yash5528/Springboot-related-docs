spring:
  cloud:
    config:
      server:
         native:
            search-locations: classpath:/shared
      #  git:
      #       uri: https://github.com/kbastani/spring-boot-microservice-config
  
  profiles:
     active: native

server:
  port: 8889

